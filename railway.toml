[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "bash scripts/pr-env-init.sh && node_modules/.bin/prisma migrate deploy && node_modules/.bin/next start -p ${PORT:-3000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
